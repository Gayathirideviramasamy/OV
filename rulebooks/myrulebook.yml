---
name: Sample Ping Rulebook
hosts: all
sources:
  - ansible.eda.generic:
      payload:
        message: "trigger"
rules:
  - name: Run ping when triggered
    condition: event.message == "trigger"
    action:
      run_playbook:
        name: test.yml
