---
name: Example Rulebook
hosts: all
sources:
  - ansible.eda.generic:
      payload:
        message: "trigger"
rules:
  - name: Example Rule
    condition: event.message == "trigger"
    action:
      run_playbook:
        name: example_playbook.yml
